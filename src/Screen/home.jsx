import SideBar from "../Component/sideBar"
import { BrowserRouter } from "react-router-dom"

const Home = () => {

    return <div>
        <BrowserRouter>
        <SideBar/>
        </BrowserRouter>
    </div>
}

export default Home

